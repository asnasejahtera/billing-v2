export type TestRouterConnectionActionState = {
  success?: boolean;
  message?: string;
  identity?: string;
};

export const initialTestRouterConnectionState:
  TestRouterConnectionActionState = {};